# Production build config for the web app (committed on purpose — this URL is
# public; it ships inside the JS bundle). Vite loads this during `vite build`,
# so Vercel always builds against the deployed Render API without needing a
# dashboard env var.
VITE_API_URL=https://gym-management-jm0b.onrender.com/api/v1
